{
  "name": "final_project_start",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "sass": "node-sass -w scss/ -o dist/css --recursive",
    "sass-once": "node-sass scss/ -o dist/css --recursive",
    "copy": "cp -r img/ dist && cp -r js/ dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-sass": "^5.0.0"
  }
}
